<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width,initial-scale=1.0" />
    <meta content="<%= csrfToken %>" name="_csrf">
    <link href="/css/styles.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <%- include("header.ejs") %>
    <div class="grid grid-cols-7 gap-6 px-4">
      <div class="col-start-3 col-span-3">
        <div class="flex justify-between items-center">
          <h6 class="py-4 text-3xl font-bold mb-4"><%= current_sport.sport_name %></h6>
          <% if (current_sport.userId == userid) { %>
          <span>
            <a href="/editsport/<%= current_sport.sport_name %>" class="mr-10 underline text-indigo-700 text-xl">Edit</a>
            <a href="/deletesport/<%= current_sport.id %>" class="underline text-indigo-700 text-xl">Delete</a>
          </span>
          <% } %>
        </div>
        <h1 class="text-2xl mb-2 font-bold">Sessions</h1>

        <% if(usersessions.length < 1 && othersessions.length < 1){ %>
          <p class="text-xl font-bold text-indigo-700 mt-2 mb-2">There are no sessions available to join right now. Create one?</p>
        <% } else{ %>  
          <%- include('sessionslist', {title:"Created by you",data: usersessions, userid, sportid:current_sport.id}) %>
          <%- include('sessionslist', {title:"Other Available Sessions",data: othersessions, userid, sportid:current_sport.id}) %>
       <% } %>

       <%- include('sessionslist', {title:"Joined Sessions",data: joinedsessions, userid, sportid:current_sport.id}) %>

        <div style="display: flex;" class="mb-8">
          <a href="/sportsession/<%= current_sport.sport_name %>" class="underline text-indigo-700 text-xl mr-20">Create a new session</a>
          <a href="/previoussession" class="underline text-indigo-700 text-xl">View previous sessions</a>
        </div>
      </div>
    </div>
  </body>
</html>
